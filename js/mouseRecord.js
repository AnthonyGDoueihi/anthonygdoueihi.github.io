const mouseMove = [];

const getPos = function(e){
  mouseMove.push()
}

document.onmousemove = function(e){
  console.log(window);
  // window.innerWidth Need to get the page width and height to get the actual relative
  // e.pageX
  // window.innerHeight
  // e.pageY

}
